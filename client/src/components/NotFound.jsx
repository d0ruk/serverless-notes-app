import React from "react";
import styles from "./NotFound.css";

export default (props, ctx) => { // eslint-disable-line
  return (
    <div className={styles.wrapper}>
      <h3>Page not found</h3>
    </div>
  );
};
